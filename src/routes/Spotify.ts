import { Router } from "express";
import { Request, Response } from 'express';


const router = Router();

router.get('/login', async function spotifyRedirect(req: Request, res: Response){
  


}));







export default router;